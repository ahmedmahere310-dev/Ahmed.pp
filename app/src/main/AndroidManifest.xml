<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".HydraApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Hydra">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".notification.ReminderBroadcastReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.hydra.app.ACTION_REMINDER" />
                <action android:name="com.hydra.app.ACTION_DONE" />
                <action android:name="com.hydra.app.ACTION_SNOOZE" />
                <action android:name="com.hydra.app.ACTION_DISMISS" />
                <action android:name="com.hydra.app.ACTION_WATER_REMINDER" />
                <action android:name="com.hydra.app.ACTION_FOOD_REMINDER" />
                <action android:name="com.hydra.app.ACTION_QUICK_WATER_250" />
                <action android:name="com.hydra.app.ACTION_QUICK_WATER_500" />
                <action android:name="com.hydra.app.ACTION_SNOOZE_WATER" />
                <action android:name="com.hydra.app.ACTION_SNOOZE_FOOD" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
